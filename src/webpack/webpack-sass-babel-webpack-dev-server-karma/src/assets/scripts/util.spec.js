import util from './util';



describe('util', () => {
    it('to be defined', () => {
        expect(util).toBeDefined();
    });

    //let result = util.run();

    // it('should work', () => {
    //     expect(result).toBe('i am util');
    // });
});